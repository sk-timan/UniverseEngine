## 1. 世界与关卡骨架

- [x] 1.1 创建 `OpenSpecTest/src/world` 模块目录与基础头/源文件（`World`、`Level`、`Actor`）。
- [x] 1.2 定义 `World` 公共接口：加载关卡、卸载关卡、获取活动关卡、逐帧 Tick。
- [x] 1.3 定义 `Level` 公共接口：状态流转（Loading/Loaded/Unloading/Unloaded）、Actor 注册与移除。
- [x] 1.4 定义 `Actor` 基类最小语义：唯一标识、名称、Transform、Tick、待销毁标记。

## 2. 生命周期与对象系统集成

- [x] 2.1 将 `World/Level/Actor` 接入现有 `UObject/ObjectRegistry`，统一对象创建与引用关系。
- [x] 2.2 实现关卡加载到激活的生命周期流程，确保单活动关卡约束成立。
- [x] 2.3 实现关卡卸载与 Actor 回收流程，确保卸载后 Actor 不再参与 Tick。
- [x] 2.4 为关卡切换添加守护逻辑（先卸载后加载）并输出可诊断错误信息。

## 3. 应用主循环接入

- [x] 3.1 在 `GameApp` 中持有 `World` 实例，替换或包裹现有直接场景驱动路径。
- [x] 3.2 在初始化阶段读取 `gameplay.world.map_id` 并触发首个关卡加载流程。
- [x] 3.3 将每帧更新链路调整为 `GameApp -> World -> Level -> Actor`。
- [x] 3.4 处理 `map_id` 缺失/无效场景，保证系统拒绝激活并给出清晰错误。

## 4. 渲染边界与关卡输入

- [x] 4.1 约束关卡层仅描述资源需求，避免 `World/Level/Actor` 直接依赖 DX12 设备细节。
- [x] 4.2 将当前地图模型路径输入从应用硬编码迁移为由活动关卡提供。
- [x] 4.3 保持渲染器职责单一（渲染执行），不承接关卡生命周期管理逻辑。

## 5. 验证与回归

- [x] 5.1 添加/更新单元测试：活动关卡唯一性、状态流转、卸载后不可更新。
- [x] 5.2 添加/更新集成验证：有效 `map_id` 可加载，非法 `map_id` 被拒绝激活。
- [x] 5.3 回归检查现有 `gameplay-config-editor` 行为，确保配置读写能力不回退。
- [x] 5.4 进行手工验收：启动、切图、退出流程无崩溃，资源与对象释放符合预期。（已补充编辑器“加载地图”入口并完成手工验收）
