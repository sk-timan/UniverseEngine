## 1. 配置读写基础设施

- [x] 1.1 在数据层新增 `gameplay` 配置存储模块（如 `GameplayConfigStore`），定义统一的 `Load/Validate/Save` 接口。
- [x] 1.2 基于 `nlohmann/json` 实现 `gameplay.json` 统一读写，覆盖 `version`、`world.map_id`、`world.spawn`、`economy.starting_coins`。
- [x] 1.3 实现旧格式读取兼容策略（缺失新字段时回退旧字段），并确保保存时统一输出新 schema。
- [x] 1.4 在 `CMake` 与包含路径中确认数据层仅依赖通用 JSON 实现，不引入 Qt JSON 解析路径。

## 2. ResourceLoader 接入与模型同步

- [x] 2.1 重构 `ResourceLoader::LoadGameplayConfig`，接入统一存储层并移除手写字符串解析主路径。
- [x] 2.2 对加载失败场景补齐结构化错误信息，保证可定位到字段或文件级问题。
- [x] 2.3 在应用启动路径中验证新加载链路，确保初始化行为与现有流程兼容。

## 3. Qt 编辑面板交互闭环

- [x] 3.1 将编辑面板从占位控件改为真实字段绑定（`map_id`、`spawn x/y/z`、`starting_coins`）。
- [x] 3.2 实现“应用”操作：从 UI 构建草稿模型、执行校验、保存成功后清理脏状态。
- [x] 3.3 实现“重置”操作：恢复到最近一次成功加载/保存的配置快照。
- [x] 3.4 实现脏状态提示与按钮联动（未保存改动时启用应用/重置并显示状态提示）。

## 4. 校验与错误反馈

- [x] 4.1 实现字段级校验规则：`map_id` 非空、`starting_coins >= 0`、`spawn` 为有效数值。
- [x] 4.2 在 UI 层展示可读且可定位的错误反馈，保存失败时禁止写入文件。
- [x] 4.3 保存成功时提供明确成功反馈并保持 UI 与文件状态一致。

## 5. 验证与回归

- [x] 5.1 手工验证：修改并保存后重启，界面显示值与文件内容一致。
- [x] 5.2 兼容验证：使用旧格式文件启动并保存，确认文件成功收敛为新 schema。
- [x] 5.3 异常验证：构造非法输入，确认保存被阻止且错误提示准确。
- [x] 5.4 补充最小回归测试（解析/序列化/兼容读取），覆盖关键字段映射与边界情况。
